import { FC } from 'react'
import { Typography } from '@mui/material'

const App: FC = () => (
  <div>
    <Typography
      component="h1"
      variant="h1"
    >
      Weather App
    </Typography>
  </div>
)

export default App
